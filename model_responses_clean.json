{
  "vllm/Qwen3.6-35B-A3B-NVFP4": {
    "coding-bug-fix": "",
    "reasoning-puzzle": "",
    "coding-algo": "",
    "reasoning-math": "$1 - \\prod_{k=0}^{22} \\frac{365 - k}{365}$\n\n0.5073",
    "comprehension-read": "### Function Explanation\nThe function groups a list of dictionaries by a `'category'` key (defaulting to `'unknown'` if missing), collects their `'value'` keys (defaulting to `0` if missing), and returns a new dictionary where each key is a category and its value is the arithmetic mean (average) of all values associated with that category.\n\n---\n\n### Answers\n\n1. **What does it return?**  \n   It returns a dictionary mapping each unique category (or `'unknown'` for missing categories) to the average of all `'value'` entries belonging to that category. For example, `{'A': 5.5, 'unknown': 2.0}`.\n\n2. **What happens if data contains an empty dict?**  \n   An empty dictionary `{}` lacks both `'category'` and `'value'` keys. The function uses the provided defaults: `'unknown'` for the category and `0` for the value. As a result, `0` is appended to the `'unknown'` category's list, which will lower the average for `'unknown'` if other values are present.\n\n3. **What happens if a category has zero items?**  \n   Under the current implementation, **this scenario is impossible**. The code only creates a category key in `result` when it processes an item, and immediately appends a value to it. Therefore, every category in the output will always have at least one item. If it theoretically occurred, the category would either not appear in the output or cause a division-by-zero error.\n\n4. **What edge case could cause a ZeroDivisionError?**  \n   A `ZeroDivisionError` would occur if `len(v) == 0` in the return statement's `sum(v) / len(v)` expression. This would happen if a category's value list ended up empty. However, due to the code's structure (initializing and immediately appending), this is **practically impossible** with the given logic. The function safely handles an empty `data` list by returning `{}`, so no division ever takes place. Only if the code were modified to pre-initialize categories without appending, or if external code mutated the lists, would this error become possible.",
    "planning-task": "1. **Database schema change (SQL)**\n   ```sql\n   CREATE TABLE IF NOT EXISTS user_theme_prefs (\n       user_id INTEGER PRIMARY KEY,\n       dark_mode BOOLEAN NOT NULL DEFAULT 0,\n       updated_at DATETIME DEFAULT (datetime('now')),\n       FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE\n   );\n   ```\n\n2. **API endpoints needed (URL + method)**\n   - `GET /api/preferences/theme` \u2192 Returns JSON payload `{ \"dark_mode\": true/false }` for the authenticated user.\n   - `PUT /api/preferences/theme` \u2192 Accepts JSON payload `{ \"dark_mode\": boolean }`, updates the SQLite record, and returns the new state.\n\n3. **Frontend components (HTML/CSS/JS) - what each does**\n   - **HTML**: A hidden `<input type=\"checkbox\" id=\"theme-toggle\">` paired with a `<label>` acts as the accessible toggle switch, triggering JavaScript on change.\n   - **CSS**: Defines CSS custom properties (variables) for light/dark palettes, applies them via a `.dark` class on `<html>`, includes a `@media (prefers-color-scheme: dark)` fallback, and styles the toggle UI with smooth transitions.\n   - **JS**: A `ThemeController` script that fetches the current preference on page load, applies `.dark` to `<html>`, listens for toggle changes to send a `PUT` request, updates `localStorage` for instant feedback, and syncs state across open tabs.\n\n4. **Default/inheritance behavior**\n   When no database record exists, the theme defaults to the browser's system preference via the CSS media query; inheritance is handled by injecting the resolved theme class once in the Jinja2 base template's `<html>` tag, automatically cascading to all child routes without per-page configuration.\n\n5. **One sentence on cross-device persistence**\n   The selected theme is stored in the SQLite database and bound to the authenticated user's account, enabling automatic synchronization and persistence across any device where the user logs in."
  },
  "azure_ai/gpt-5.6-luna": {
    "coding-bug-fix": "from typing import TypeVar\n\nT = TypeVar(\"T\")\n\ndef last_n_unique(lst: list[T], n: int) -> list[T]:\n    \"\"\"Return the last n unique elements in their original list order.\"\"\"\n    if n <= 0:\n        return []\n\n    seen: list[T] = []\n    for item in reversed(lst):\n        if item not in seen:\n            seen.append(item)\n            if len(seen) == n:\n                break\n\n    return list(reversed(seen))",
    "reasoning-puzzle": "step 1: C\u2192A \u2192 (5, 0, 3)  \nstep 2: A\u2192B \u2192 (2, 3, 3)  \nstep 3: B\u2192C \u2192 (2, 0, 6)  \nstep 4: A\u2192B \u2192 (0, 2, 6)  \nstep 5: C\u2192A \u2192 (5, 2, 1)  \nstep 6: A\u2192B \u2192 (4, 3, 1)  \nstep 7: B\u2192C \u2192 (4, 0, 4)  \n\nFinal count: 7",
    "coding-algo": "from collections import OrderedDict\n\nclass LRUCache:\n    def __init__(self, capacity: int):\n        if capacity <= 0:\n            raise ValueError(\"capacity must be positive\")\n        self.capacity = capacity\n        self._cache = OrderedDict()\n\n    def get(self, key: int) -> int:\n        if key not in self._cache:\n            return -1\n\n        value = self._cache.pop(key)\n        self._cache[key] = value\n        return value\n\n    def put(self, key: int, value: int) -> None:\n        if key in self._cache:\n            self._cache.pop(key)\n\n        self._cache[key] = value\n\n        if len(self._cache) > self.capacity:\n            self._cache.popitem(last=False)\n\n    def __repr__(self) -> str:\n        pairs = \", \".join(\n            f\"({key}, {value})\" for key, value in self._cache.items()\n        )\n        return f\"LRUCache([{pairs}])\"",
    "reasoning-math": "\\[\nP(\\text{at least two share a birthday})\n=1-P(\\text{all birthdays are different})\n\\]\n\n\\[\n=1-\\left(\\frac{365}{365}\\right)\\left(\\frac{364}{365}\\right)\\left(\\frac{363}{365}\\right)\\cdots\\left(\\frac{343}{365}\\right)\n\\]\n\n\\[\n=1-\\prod_{k=0}^{22}\\frac{365-k}{365}\n\\]\n\n\\[\n\\boxed{P\\approx 0.5073}\n\\]",
    "comprehension-read": "1. It returns a dictionary mapping each category to the average of its values. If an item lacks `category`, it uses `'unknown'`; if it lacks `value`, it uses `0`. For example:\n\n   ```python\n   process([\n       {'category': 'A', 'value': 10},\n       {'category': 'A', 'value': 20},\n       {'category': 'B', 'value': 5},\n   ])\n   # {'A': 15.0, 'B': 5.0}\n   ```\n\n2. An empty dictionary is treated as `{'category': 'unknown', 'value': 0}`. It contributes `0` to the average for the `'unknown'` category.\n\n3. A category with zero items does not appear in the result. The function only creates categories when it processes an item, so it never attempts to average an empty list.\n\n4. A `ZeroDivisionError` could occur only if `result` somehow contained a category mapped to an empty list when the final comprehension runs\u2014for example, if the function were modified to create empty category entries. With ordinary `list[dict]` input, this function cannot produce such an empty list, so zero division does not occur.",
    "planning-task": "1. **Database schema change**\n\n   Create a separate preferences table linked to the existing `users` table:\n\n   ```sql\n   CREATE TABLE IF NOT EXISTS user_preferences (\n       user_id INTEGER PRIMARY KEY,\n       theme TEXT NOT NULL DEFAULT 'system'\n           CHECK (theme IN ('system', 'light', 'dark')),\n       updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,\n       FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE\n   );\n   ```\n\n   The values mean:\n\n   - `system`: inherit the operating system or browser preference.\n   - `light`: always use the light theme.\n   - `dark`: always use the dark theme.\n\n2. **API endpoints needed**\n\n   1. `GET /api/me/preferences`\n\n      Returns the authenticated user\u2019s saved preference and the currently effective theme.\n\n      Example response:\n\n      ```json\n      {\n        \"theme\": \"dark\",\n        \"effective_theme\": \"dark\"\n      }\n      ```\n\n   2. `PATCH /api/me/preferences`\n\n      Updates the authenticated user\u2019s theme preference.\n\n      Request:\n\n      ```json\n      {\n        \"theme\": \"light\"\n      }\n      ```\n\n      Accepted values are `system`, `light`, and `dark`.\n\n   3. `DELETE /api/me/preferences/theme`\n\n      Removes the explicit preference and resets the user to the default `system` behavior.\n\n   4. Optional: `GET /api/theme-config`\n\n      Returns public theme configuration, such as the application default. This is useful if theme behavior is configurable by an administrator, but it is not required if the default is always `system`.\n\n3. **Frontend components**\n\n   1. **Theme toggle HTML**\n\n      Add the control to the shared Jinja2 layout, such as `base.html`:\n\n      ```html\n      <button\n          id=\"theme-toggle\"\n          type=\"button\"\n          aria-label=\"Toggle dark mode\"\n          aria-pressed=\"false\">\n          <span class=\"theme-toggle__icon\" aria-hidden=\"true\">\ud83c\udf19</span>\n          <span class=\"theme-toggle__label\">Dark mode</span>\n      </button>\n      ```\n\n   2. **Theme-aware CSS**\n\n      Define colors as CSS variables and switch them using a `data-theme` attribute:\n\n      ```css\n      :root {\n          color-scheme: light dark;\n          --background: #ffffff;\n          --text: #1f2937;\n          --surface: #f3f4f6;\n          --border: #d1d5db;\n      }\n\n      :root[data-theme=\"light\"] {\n          color-scheme: light;\n          --background: #ffffff;\n          --text: #1f2937;\n          --surface: #f3f4f6;\n          --border: #d1d5db;\n      }\n\n      :root[data-theme=\"dark\"] {\n          color-scheme: dark;\n          --background: #111827;\n          --text: #f9fafb;\n          --surface: #1f2937;\n          --border: #4b5563;\n      }\n\n      body {\n          background: var(--background);\n          color: var(--text);\n      }\n\n      .card,\n      .navbar {\n          background: var(--surface);\n          border-color: var(--border);\n      }\n      ```\n\n      Components should use the variables rather than hard-coded colors.\n\n   3. **Early theme initialization script**\n\n      Place a small inline script in the `<head>` so the correct theme is applied before the page renders:\n\n      ```html\n      <script>\n      (() => {\n          const saved = localStorage.getItem(\"theme\");\n          const systemDark = window.matchMedia(\"(prefers-color-scheme: dark)\").matches;\n          const theme = saved === \"light\" || saved === \"dark\"\n              ? saved\n              : (systemDark ? \"dark\" : \"light\");\n\n          document.documentElement.dataset.theme = theme;\n      })();\n      </script>\n      ```\n\n      This prevents a light-mode flash before JavaScript loads.\n\n   4. **Vanilla JavaScript controller**\n\n      The JavaScript should:\n\n      - Read the current preference from the API for authenticated users.\n      - Fall back to `localStorage` for anonymous users.\n      - Set `document.documentElement.dataset.theme`.\n      - Update the button label, icon, and `aria-pressed`.\n      - Listen for `prefers-color-scheme` changes when the preference is `system`.\n      - Send changes to `PATCH /api/me/preferences`.\n      - Revert to the previous setting or display an error if the API request fails.\n\n      Example logic:\n\n      ```javascript\n      const toggle = document.querySelector(\"#theme-toggle\");\n      const root = document.documentElement;\n\n      function applyTheme(theme) {\n          const effective = theme === \"system\"\n              ? (matchMedia(\"(prefers-color-scheme: dark)\").matches ? \"dark\" : \"light\")\n              : theme;\n\n          root.dataset.theme = effective;\n          toggle.setAttribute(\"aria-pressed\", effective === \"dark\");\n          toggle.querySelector(\".theme-toggle__label\").textContent =\n              effective === \"dark\" ? \"Light mode\" : \"Dark mode\";\n      }\n\n      toggle.addEventListener(\"click\", async () => {\n          const nextTheme = root.dataset.theme === \"dark\" ? \"light\" : \"dark\";\n\n          applyTheme(nextTheme);\n          localStorage.setItem(\"theme\", nextTheme);\n\n          await fetch(\"/api/me/preferences\", {\n              method: \"PATCH\",\n              headers: { \"Content-Type\": \"application/json\" },\n              body: JSON.stringify({ theme: nextTheme })\n          });\n      });\n\n      matchMedia(\"(prefers-color-scheme: dark)\")\n          .addEventListener(\"change\", () => {\n              if (!localStorage.getItem(\"theme\")) {\n                  applyTheme(\"system\");\n              }\n          });\n      ```\n\n      In production, the authenticated-user flow should use the server preference as the source of truth and use `localStorage` only as an initial, anonymous, or offline fallback.\n\n4. **Default and inheritance behavior**\n\n   1. A new user has no row in `user_preferences`, or has `theme = 'system'`.\n   2. The effective default is the browser or operating system preference.\n   3. An explicit `light` or `dark` setting overrides the system preference.\n   4. Setting the preference to `system`, or calling `DELETE /api/me/preferences/theme`, removes the explicit override.\n   5. Anonymous users use `localStorage` and the system preference; their setting is not saved to SQLite.\n   6. If the API is unavailable, the interface continues using the last local setting and can synchronize it later.\n\n5. **Cross-device persistence**\n\n   For authenticated users, the explicit theme preference is stored in SQLite and therefore follows them across devices, while anonymous users only retain their setting in the current browser."
  },
  "azure_ai/gpt-5.6-sol": {
    "coding-bug-fix": "from typing import TypeVar\n\nT = TypeVar(\"T\")\n\ndef last_n_unique(lst: list[T], n: int) -> list[T]:\n    \"\"\"Return the last n unique elements, preserving their original order.\"\"\"\n    if n <= 0:\n        return []\n\n    seen: list[T] = []\n    for item in reversed(lst):\n        if item not in seen:\n            seen.append(item)\n            if len(seen) == n:\n                break\n\n    return seen[::-1]",
    "reasoning-puzzle": "step 1: C\u2192A \u2192 (5, 0, 3)  \nstep 2: A\u2192B \u2192 (2, 3, 3)  \nstep 3: B\u2192C \u2192 (2, 0, 6)  \nstep 4: A\u2192B \u2192 (0, 2, 6)  \nstep 5: C\u2192A \u2192 (5, 2, 1)  \nstep 6: A\u2192B \u2192 (4, 3, 1)  \nstep 7: B\u2192C \u2192 (4, 0, 4)  \n\nFinal count: 7",
    "coding-algo": "class LRUCache:\n    class _Node:\n        __slots__ = (\"key\", \"value\", \"prev\", \"next\")\n\n        def __init__(self, key: int = 0, value: int = 0) -> None:\n            self.key = key\n            self.value = value\n            self.prev = None\n            self.next = None\n\n    def __init__(self, capacity: int):\n        if capacity < 0:\n            raise ValueError(\"capacity must be non-negative\")\n\n        self.capacity = capacity\n        self._cache = {}\n        self._head = self._Node()  # Least recently used side\n        self._tail = self._Node()  # Most recently used side\n        self._head.next = self._tail\n        self._tail.prev = self._head\n\n    def get(self, key: int) -> int:\n        node = self._cache.get(key)\n        if node is None:\n            return -1\n\n        self._remove(node)\n        self._append(node)\n        return node.value\n\n    def put(self, key: int, value: int) -> None:\n        node = self._cache.get(key)\n\n        if node is not None:\n            node.value = value\n            self._remove(node)\n            self._append(node)\n            return\n\n        if self.capacity == 0:\n            return\n\n        node = self._Node(key, value)\n        self._cache[key] = node\n        self._append(node)\n\n        if len(self._cache) > self.capacity:\n            lru = self._head.next\n            self._remove(lru)\n            del self._cache[lru.key]\n\n    def _remove(self, node: _Node) -> None:\n        node.prev.next = node.next\n        node.next.prev = node.prev\n\n    def _append(self, node: _Node) -> None:\n        previous = self._tail.prev\n        previous.next = node\n        node.prev = previous\n        node.next = self._tail\n        self._tail.prev = node\n\n    def __repr__(self) -> str:\n        pairs = []\n        node = self._head.next\n\n        while node is not self._tail:\n            pairs.append((node.key, node.value))\n            node = node.next\n\n        return f\"{self.__class__.__name__}({pairs!r})\"",
    "reasoning-math": "\\[\nP(\\text{at least two share a birthday})\n=1-\\prod_{k=0}^{22}\\frac{365-k}{365}\n=1-\\left(\\frac{365}{365}\\cdot\\frac{364}{365}\\cdot\\frac{363}{365}\\cdots\\frac{343}{365}\\right)\n\\approx \\boxed{0.5073}\n\\]",
    "comprehension-read": "1. It returns a dictionary mapping each category to the arithmetic mean of its items\u2019 `value` fields. Missing categories default to `'unknown'`, and missing values default to `0`.\n\n2. An empty dictionary is grouped under `'unknown'` with a value of `0`, producing or contributing to an average for `'unknown'`.\n\n3. A category cannot have zero items in this implementation: a category is added to `result` only when an item is immediately appended to its list. If `data` is empty, the function returns `{}`.\n\n4. As written, no normal input can cause a `ZeroDivisionError`, because every list in `result` contains at least one value. It would occur only if an empty list somehow appeared in `result` before the final comprehension, making `len(v) == 0`.",
    "planning-task": "1. **Database schema change**\n\n   Create a one-to-one preferences table so additional user preferences can be added later:\n\n   ```sql\n   PRAGMA foreign_keys = ON;\n\n   CREATE TABLE user_preferences (\n       user_id INTEGER PRIMARY KEY,\n       theme TEXT NOT NULL DEFAULT 'system'\n           CHECK (theme IN ('system', 'light', 'dark')),\n       updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,\n       FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE\n   );\n\n   CREATE TRIGGER user_preferences_set_updated_at\n   AFTER UPDATE ON user_preferences\n   FOR EACH ROW\n   BEGIN\n       UPDATE user_preferences\n       SET updated_at = CURRENT_TIMESTAMP\n       WHERE user_id = OLD.user_id;\n   END;\n   ```\n\n   Existing users without a row inherit the `system` default. Use an upsert when saving:\n\n   ```sql\n   INSERT INTO user_preferences (user_id, theme)\n   VALUES (?, ?)\n   ON CONFLICT(user_id) DO UPDATE SET theme = excluded.theme;\n   ```\n\n2. **API endpoints**\n\n   1. `GET /api/me/preferences/theme`\n      - Requires authentication.\n      - Returns the stored preference, defaulting to `system` when no row exists.\n      - Example response:\n\n        ```json\n        { \"theme\": \"system\" }\n        ```\n\n   2. `PUT /api/me/preferences/theme`\n      - Requires authentication and normal CSRF protection if cookie-based sessions are used.\n      - Accepts and validates one of `system`, `light`, or `dark`.\n      - Example request:\n\n        ```json\n        { \"theme\": \"dark\" }\n        ```\n\n      - Returns the saved value:\n\n        ```json\n        { \"theme\": \"dark\" }\n        ```\n\n   3. Include the preference in the normal Jinja page context, such as `theme_preference`, so initial rendering does not need to wait for the GET endpoint.\n\n3. **Frontend components**\n\n   1. **HTML**\n      - Add the saved preference to the root element:\n\n        ```html\n        <html lang=\"en\" data-theme=\"{{ theme_preference | default('system') }}\">\n        ```\n\n      - Add an accessible toggle plus an optional control for restoring system inheritance:\n\n        ```html\n        <button id=\"theme-toggle\"\n                type=\"button\"\n                aria-pressed=\"false\"\n                aria-label=\"Enable dark mode\">\n          <span aria-hidden=\"true\">\ud83c\udf19</span>\n          <span class=\"theme-toggle-label\">Dark mode</span>\n        </button>\n\n        <button id=\"theme-system\" type=\"button\">\n          Use device setting\n        </button>\n        ```\n\n   2. **CSS**\n      - Convert existing hard-coded colors to custom properties:\n\n        ```css\n        :root,\n        html[data-theme=\"light\"] {\n          color-scheme: light;\n          --background: #ffffff;\n          --surface: #f5f5f5;\n          --text: #171717;\n          --muted-text: #666666;\n          --border: #dddddd;\n          --link: #005fcc;\n        }\n\n        html[data-theme=\"dark\"] {\n          color-scheme: dark;\n          --background: #121212;\n          --surface: #1e1e1e;\n          --text: #eeeeee;\n          --muted-text: #aaaaaa;\n          --border: #444444;\n          --link: #8ab4f8;\n        }\n\n        @media (prefers-color-scheme: dark) {\n          html[data-theme=\"system\"] {\n            color-scheme: dark;\n            --background: #121212;\n            --surface: #1e1e1e;\n            --text: #eeeeee;\n            --muted-text: #aaaaaa;\n            --border: #444444;\n            --link: #8ab4f8;\n          }\n        }\n\n        body {\n          background: var(--background);\n          color: var(--text);\n        }\n        ```\n\n      - Update buttons, forms, cards, borders, links, and other components to use these variables.\n      - Optionally disable transitions during initial page load to avoid a visible theme flash.\n\n   3. **JavaScript**\n      - Read `document.documentElement.dataset.theme`.\n      - Determine the effective theme with `matchMedia(\"(prefers-color-scheme: dark)\")` when the preference is `system`.\n      - Update `aria-pressed`, icon, and label to reflect the effective theme.\n      - On toggle, switch between explicit `light` and `dark`, update the DOM immediately, and send a `PUT` request.\n      - On \u201cUse device setting,\u201d set the value to `system` and persist it.\n      - Listen for operating-system theme changes while the selected preference is `system`.\n      - Roll back the UI or show an error if persistence fails.\n\n        ```js\n        const root = document.documentElement;\n        const toggle = document.querySelector(\"#theme-toggle\");\n        const systemButton = document.querySelector(\"#theme-system\");\n        const media = window.matchMedia(\"(prefers-color-scheme: dark)\");\n\n        function effectiveTheme() {\n          const preference = root.dataset.theme || \"system\";\n          return preference === \"system\"\n            ? (media.matches ? \"dark\" : \"light\")\n            : preference;\n        }\n\n        function updateControl() {\n          const dark = effectiveTheme() === \"dark\";\n          toggle.setAttribute(\"aria-pressed\", String(dark));\n          toggle.setAttribute(\n            \"aria-label\",\n            dark ? \"Enable light mode\" : \"Enable dark mode\"\n          );\n        }\n\n        async function saveTheme(theme) {\n          const previous = root.dataset.theme;\n          root.dataset.theme = theme;\n          updateControl();\n\n          try {\n            const response = await fetch(\"/api/me/preferences/theme\", {\n              method: \"PUT\",\n              headers: {\n                \"Content-Type\": \"application/json\",\n                // Add the application's CSRF header here if required.\n              },\n              body: JSON.stringify({ theme })\n            });\n\n            if (!response.ok) throw new Error(\"Unable to save theme\");\n          } catch (error) {\n            root.dataset.theme = previous;\n            updateControl();\n            // Replace with the application's error notification UI.\n            console.error(error);\n          }\n        }\n\n        toggle.addEventListener(\"click\", () => {\n          saveTheme(effectiveTheme() === \"dark\" ? \"light\" : \"dark\");\n        });\n\n        systemButton.addEventListener(\"click\", () => saveTheme(\"system\"));\n\n        media.addEventListener(\"change\", () => {\n          if (root.dataset.theme === \"system\") updateControl();\n        });\n\n        updateControl();\n        ```\n\n4. **Default and inheritance behavior**\n\n   1. A missing preferences row or a stored value of `system` inherits the browser/operating-system `prefers-color-scheme` setting.\n   2. If the browser does not support that media query, `system` falls back to light mode.\n   3. An explicit `light` or `dark` value overrides the system setting.\n   4. Changing the system theme updates the page automatically only when the user preference remains `system`.\n   5. The Jinja-rendered `data-theme` value should be applied before the main stylesheet loads to minimize flashes of the wrong theme.\n\n5. **Cross-device persistence**\n\n   1. Because the preference is stored against the authenticated user in SQLite, the selected theme follows that user across devices after login."
  },
  "azure_ai/gpt-5.4-mini-copilot": {
    "coding-bug-fix": "```python\nfrom typing import List, TypeVar\n\nT = TypeVar(\"T\")\n\ndef last_n_unique(lst: List[T], n: int) -> List[T]:\n    \"\"\"Return the last n unique elements from lst, preserving their original order.\"\"\"\n    seen = []\n    for x in reversed(lst):\n        if x not in seen:\n            seen.append(x)\n    return list(reversed(seen[:n]))\n```",
    "reasoning-puzzle": "step 1: C\u2192A \u2192 (5, 0, 3)  \nstep 2: A\u2192B \u2192 (2, 3, 3)  \nstep 3: B\u2192C \u2192 (2, 0, 6)  \nstep 4: A\u2192B \u2192 (0, 2, 6)  \nstep 5: C\u2192A \u2192 (5, 2, 1)  \nstep 6: A\u2192B \u2192 (4, 3, 1)  \nstep 7: B\u2192C \u2192 (4, 0, 4)  \n\nFinal count: 7",
    "coding-algo": "from collections import OrderedDict\n\nclass LRUCache:\n    def __init__(self, capacity: int):\n        self.capacity = capacity\n        self.cache = OrderedDict()\n\n    def get(self, key: int) -> int:\n        if key not in self.cache:\n            return -1\n        self.cache.move_to_end(key)\n        return self.cache[key]\n\n    def put(self, key: int, value: int) -> None:\n        if key in self.cache:\n            self.cache.move_to_end(key)\n        self.cache[key] = value\n        if len(self.cache) > self.capacity:\n            self.cache.popitem(last=False)\n\n    def __repr__(self):\n        items = \", \".join(f\"({k}, {v})\" for k, v in self.cache.items())\n        return f\"LRUCache([{items}])\"",
    "reasoning-math": "\\[\nP(\\text{at least one shared birthday}) \\;=\\; 1 - P(\\text{all birthdays different})\n\\]\n\n\\[\nP(\\text{all different}) \\;=\\; \\frac{365}{365}\\cdot\\frac{364}{365}\\cdot\\frac{363}{365}\\cdot\\frac{362}{365}\\cdot\\frac{361}{365}\\cdot\\frac{360}{365}\\cdot\\frac{359}{365}\\cdot\\frac{358}{365}\\cdot\\frac{357}{365}\\cdot\\frac{356}{365}\\cdot\\frac{355}{365}\\cdot\\frac{354}{365}\\cdot\\frac{353}{365}\\cdot\\frac{352}{365}\\cdot\\frac{351}{365}\\cdot\\frac{350}{365}\\cdot\\frac{349}{365}\\cdot\\frac{348}{365}\\cdot\\frac{347}{365}\\cdot\\frac{346}{365}\\cdot\\frac{345}{365}\\cdot\\frac{344}{365}\\cdot\\frac{343}{365}\n\\]\n\n\\[\nP(\\text{at least one shared birthday})\n= 1 - \\left(\\frac{365}{365}\\cdot\\frac{364}{365}\\cdot\\frac{363}{365}\\cdot\\frac{362}{365}\\cdot\\frac{361}{365}\\cdot\\frac{360}{365}\\cdot\\frac{359}{365}\\cdot\\frac{358}{365}\\cdot\\frac{357}{365}\\cdot\\frac{356}{365}\\cdot\\frac{355}{365}\\cdot\\frac{354}{365}\\cdot\\frac{353}{365}\\cdot\\frac{352}{365}\\cdot\\frac{351}{365}\\cdot\\frac{350}{365}\\cdot\\frac{349}{365}\\cdot\\frac{348}{365}\\cdot\\frac{347}{365}\\cdot\\frac{346}{365}\\cdot\\frac{345}{365}\\cdot\\frac{344}{365}\\cdot\\frac{343}{365}\\right)\n\\]\n\n\\[\n= 0.5073\n\\]",
    "comprehension-read": "1. **What does it return?**  \n   It returns a dictionary mapping each `category` to the **average** of the `value`s for items in that category.  \n   - If an item is missing `category`, it uses `\"unknown\"`.\n   - If an item is missing `value`, it uses `0`.\n\n2. **What happens if `data` contains an empty dict?**  \n   An empty dict contributes one item to the `\"unknown\"` category with value `0`, because:\n   - `item.get('category', 'unknown')` \u2192 `\"unknown\"`\n   - `item.get('value', 0)` \u2192 `0`\n\n3. **What happens if a category has zero items?**  \n   In the code as written, this should not happen for categories that appear in `result`, because a category is only added when an item for it is encountered.  \n   If somehow a category existed with an empty list, the final `sum(v) / len(v)` would attempt division by zero.\n\n4. **What edge case could cause a `ZeroDivisionError`?**  \n   The main edge case is if `result` contains a category mapped to an empty list, so `len(v) == 0` during the average calculation.  \n   This could happen if the code were modified elsewhere to create empty category lists, or if invalid/intermediate data somehow produced one.",
    "planning-task": "1. **Database schema change (SQL)**  \n   Add a user preference field to store the theme choice:\n   ```sql\n   ALTER TABLE users ADD COLUMN theme_preference TEXT NOT NULL DEFAULT 'system';\n   ```\n   If you want to allow only valid values, use application-level validation for:\n   - `system`\n   - `light`\n   - `dark`\n\n   If you prefer a separate preferences table instead of modifying `users`:\n   ```sql\n   CREATE TABLE user_preferences (\n       user_id INTEGER PRIMARY KEY,\n       theme_preference TEXT NOT NULL DEFAULT 'system',\n       FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE\n   );\n   ```\n\n2. **API endpoints needed (URL + method)**  \n   1. `GET /`  \n      Renders the page with the current theme applied from the user preference or fallback logic.\n   2. `GET /api/theme`  \n      Returns the current theme preference for the signed-in user, e.g. `{ \"theme\": \"dark\" }`.\n   3. `POST /api/theme`  \n      Updates the user\u2019s preference, e.g. body: `{ \"theme\": \"dark\" }`.\n   4. `GET /api/theme/system` *(optional but useful)*  \n      Returns whether the browser/system prefers dark mode, used for \u201csystem\u201d mode behavior.\n\n3. **Frontend components (HTML/CSS/JS) - what each does**  \n   1. **HTML base template (`base.html`)**\n      - Includes a theme toggle button/switch.\n      - Adds a theme class or `data-theme` attribute on the `<html>` or `<body>` element.\n      - Loads a small inline script early to avoid flash of incorrect theme.\n   2. **CSS variables / theme styles**\n      - Define colors as CSS custom properties in `:root`.\n      - Override them for dark mode with something like `[data-theme=\"dark\"]`.\n      - Keep component styling theme-agnostic by referencing variables only.\n   3. **Vanilla JS toggle script**\n      - Reads the current theme from the server or local state.\n      - Sets `data-theme=\"dark\"` or `data-theme=\"light\"` on the document root.\n      - Listens for toggle clicks and sends `POST /api/theme` to persist the choice.\n      - If user selects `system`, uses `window.matchMedia('(prefers-color-scheme: dark)')`.\n   4. **Optional inline boot script**\n      - Runs before page paint.\n      - Applies the saved theme immediately to reduce visual flash.\n   5. **Toggle UI component**\n      - A button or switch with labels/icons for light/dark/system.\n      - Reflects current state when the page loads.\n\n4. **Default/inheritance behavior**  \n   - If the user has an explicit preference (`light` or `dark`), use that.  \n   - If the preference is `system` or missing, inherit from the browser/OS `prefers-color-scheme`.  \n   - If the user is not logged in, default to `system` or a site-wide default such as `light`.  \n   - Child pages should inherit the theme from the base template, so every page stays consistent.\n\n5. **One sentence on cross-device persistence**  \n   Because the theme is stored in the user\u2019s database record, the same preference follows the user across devices and browsers after login."
  },
  "azure_ai/gpt-5.4-mini": {
    "coding-bug-fix": "```python\nfrom typing import Any, List\n\ndef last_n_unique(lst: List[Any], n: int) -> List[Any]:\n    \"\"\"Return the last n unique elements from a list, preserving their original order.\"\"\"\n    seen = []\n    for x in reversed(lst):\n        if x not in seen:\n            seen.append(x)\n    return list(reversed(seen[:n]))\n```",
    "reasoning-puzzle": "step 1: C\u2192A \u2192 (5, 0, 3)  \nstep 2: A\u2192B \u2192 (2, 3, 3)  \nstep 3: B\u2192C \u2192 (2, 0, 6)  \nstep 4: A\u2192B \u2192 (0, 2, 6)  \nstep 5: C\u2192A \u2192 (5, 2, 1)  \nstep 6: A\u2192B \u2192 (4, 3, 1)  \nstep 7: B\u2192C \u2192 (4, 0, 4)  \n\nFinal count: 7",
    "coding-algo": "from collections import OrderedDict\n\nclass LRUCache:\n    def __init__(self, capacity: int):\n        self.capacity = capacity\n        self._cache = OrderedDict()\n\n    def get(self, key: int) -> int:\n        if key not in self._cache:\n            return -1\n        self._cache.move_to_end(key)\n        return self._cache[key]\n\n    def put(self, key: int, value: int) -> None:\n        if key in self._cache:\n            self._cache.move_to_end(key)\n        self._cache[key] = value\n        if len(self._cache) > self.capacity:\n            self._cache.popitem(last=False)\n\n    def __repr__(self) -> str:\n        items = \", \".join(f\"({k}, {v})\" for k, v in self._cache.items())\n        return f\"LRUCache([{items}])\"",
    "reasoning-math": "\\[\nP(\\text{at least two share a birthday})=1-P(\\text{all birthdays different})\n\\]\n\n\\[\nP(\\text{all different})=\\frac{365}{365}\\cdot\\frac{364}{365}\\cdot\\frac{363}{365}\\cdot\\frac{362}{365}\\cdot\\frac{361}{365}\\cdot\\frac{360}{365}\\cdot\\frac{359}{365}\\cdot\\frac{358}{365}\\cdot\\frac{357}{365}\\cdot\\frac{356}{365}\\cdot\\frac{355}{365}\\cdot\\frac{354}{365}\\cdot\\frac{353}{365}\\cdot\\frac{352}{365}\\cdot\\frac{351}{365}\\cdot\\frac{350}{365}\\cdot\\frac{349}{365}\\cdot\\frac{348}{365}\\cdot\\frac{347}{365}\\cdot\\frac{346}{365}\\cdot\\frac{345}{365}\\cdot\\frac{344}{365}\n\\]\n\n\\[\nP(\\text{at least two share a birthday})\n=1-\\left(\\frac{365}{365}\\cdot\\frac{364}{365}\\cdot\\frac{363}{365}\\cdot\\frac{362}{365}\\cdot\\frac{361}{365}\\cdot\\frac{360}{365}\\cdot\\frac{359}{365}\\cdot\\frac{358}{365}\\cdot\\frac{357}{365}\\cdot\\frac{356}{365}\\cdot\\frac{355}{365}\\cdot\\frac{354}{365}\\cdot\\frac{353}{365}\\cdot\\frac{352}{365}\\cdot\\frac{351}{365}\\cdot\\frac{350}{365}\\cdot\\frac{349}{365}\\cdot\\frac{348}{365}\\cdot\\frac{347}{365}\\cdot\\frac{346}{365}\\cdot\\frac{345}{365}\\cdot\\frac{344}{365}\\right)\n\\]\n\n\\[\n=1-0.4927=0.5073\n\\]",
    "comprehension-read": "1. **What does it return?**  \n   It groups the input dictionaries by their `\"category\"` value, collects each item\u2019s `\"value\"` into a list for that category, and then returns a new dictionary mapping each category to the **average** of its values (`sum(values) / len(values)`).  \n   If `\"category\"` is missing, it uses `\"unknown\"`. If `\"value\"` is missing, it uses `0`.\n\n2. **What happens if `data` contains an empty dict?**  \n   An empty dict contributes to the `\"unknown\"` category with a value of `0`, because:\n   - `item.get('category', 'unknown')` \u2192 `\"unknown\"`\n   - `item.get('value', 0)` \u2192 `0`  \n   So it affects the average for `\"unknown\"`.\n\n3. **What happens if a category has zero items?**  \n   In normal use, this cannot happen for a category that exists in `result`, because a category is only added when at least one item is seen. So no empty category bucket is produced by this function.\n\n4. **What edge case could cause a `ZeroDivisionError`?**  \n   A `ZeroDivisionError` would happen if a category key somehow ends up in `result` with an empty list, since the final line does `sum(v) / len(v)` and `len(v)` would be `0`.  \n   With the code exactly as written, that would require unusual external mutation of `result` or some modification of the function; under normal execution with the given loop, it should not occur.",
    "planning-task": "1. **Database schema change (SQL)**  \n   Add a user preference column to store the theme choice:\n   ```sql\n   ALTER TABLE users ADD COLUMN theme_preference TEXT NOT NULL DEFAULT 'system';\n   ```\n   Recommended allowed values: `'light'`, `'dark'`, `'system'`  \n   If you don\u2019t already have a `users` table, the relevant column definition would be:\n   ```sql\n   theme_preference TEXT NOT NULL DEFAULT 'system'\n   ```\n\n2. **API endpoints needed (URL + method)**  \n   1. `GET /`  \n      - Renders the page with the current theme preference available to the template.\n   2. `POST /api/theme`  \n      - Updates the authenticated user\u2019s theme preference in SQLite.  \n      - Body example: `{ \"theme\": \"dark\" }`\n   3. `GET /api/theme`  \n      - Returns the current user\u2019s saved theme preference as JSON, useful for client-side initialization or AJAX refresh.\n   4. `GET /login` and `POST /login` or existing auth routes  \n      - Needed only insofar as theme preferences are tied to a user account and authentication.\n\n3. **Frontend components (HTML/CSS/JS) - what each does**  \n   1. **HTML**\n      - Add a toggle control in a shared layout/header, e.g. a button or switch:\n        ```html\n        <button id=\"theme-toggle\" aria-label=\"Toggle dark mode\">\ud83c\udf19</button>\n        ```\n      - Add a root theme marker on the `<html>` or `<body>` element:\n        ```html\n        <html data-theme=\"{{ theme_preference }}\">\n        ```\n   2. **CSS**\n      - Define theme-specific variables using attributes or classes:\n        ```css\n        :root {\n          --bg: #ffffff;\n          --text: #111111;\n        }\n\n        [data-theme=\"dark\"] {\n          --bg: #111111;\n          --text: #f5f5f5;\n        }\n\n        body {\n          background: var(--bg);\n          color: var(--text);\n        }\n        ```\n      - Keep all styling tied to CSS variables so switching theme updates the whole app consistently.\n   3. **Vanilla JS**\n      - On page load, read the current theme from the DOM or `/api/theme`.\n      - Toggle the theme by updating the `data-theme` attribute on `<html>` or `<body>`.\n      - Send the new preference to `POST /api/theme` so it persists:\n        ```js\n        document.getElementById(\"theme-toggle\").addEventListener(\"click\", async () => {\n          const current = document.documentElement.dataset.theme;\n          const next = current === \"dark\" ? \"light\" : \"dark\";\n          document.documentElement.dataset.theme = next;\n\n          await fetch(\"/api/theme\", {\n            method: \"POST\",\n            headers: { \"Content-Type\": \"application/json\" },\n            body: JSON.stringify({ theme: next })\n          });\n        });\n        ```\n      - Optionally use `localStorage` as a temporary fallback before the server preference loads, but keep SQLite as the source of truth.\n\n4. **Default/inheritance behavior**  \n   1. If the user has no saved preference, default to `system`.\n   2. If set to `system`, inherit from the browser/OS setting using `prefers-color-scheme`.\n   3. If a user explicitly selects `light` or `dark`, that choice overrides system behavior.\n   4. Apply the theme at the base layout level so all templates inherit it automatically.\n\n5. **One sentence on cross-device persistence**  \n   Because the preference is stored in the user\u2019s SQLite-backed account record, the same theme choice follows the user across devices after login."
  },
  "azure_ai/gpt-5-mini": {
    "coding-bug-fix": "from typing import List, TypeVar\n\nT = TypeVar(\"T\")\n\ndef last_n_unique(lst: List[T], n: int) -> List[T]:\n    \"\"\"\n    Return the last `n` unique elements from `lst`, preserving their original order.\n\n    \"Last\" refers to each element's final occurrence in the list. For example:\n      last_n_unique([1, 2, 3, 2, 4], 3) -> [3, 2, 4]\n\n    If there are fewer than `n` distinct elements, all distinct elements are returned\n    in their original order. If `n` is non-positive, an empty list is returned.\n    \"\"\"\n    if n <= 0:\n        return []\n\n    seen = []\n    seen_set = set()\n    for x in reversed(lst):\n        if x not in seen_set:\n            seen.append(x)\n            seen_set.add(x)\n            if len(seen) == n:\n                break\n\n    return list(reversed(seen))",
    "reasoning-puzzle": "step 1: C\u2192A \u2192 (5, 0, 3)\nstep 2: A\u2192B \u2192 (2, 3, 3)\nstep 3: B\u2192C \u2192 (2, 0, 6)\nstep 4: A\u2192B \u2192 (0, 2, 6)\nstep 5: C\u2192A \u2192 (5, 2, 1)\nstep 6: A\u2192B \u2192 (4, 3, 1)\nstep 7: B\u2192C \u2192 (4, 0, 4)\n\nFinal count: 7 pours",
    "coding-algo": "class LRUCache:\n    class _Node:\n        __slots__ = (\"key\", \"val\", \"prev\", \"next\")\n\n        def __init__(self, key=None, val=None):\n            self.key = key\n            self.val = val\n            self.prev = None\n            self.next = None\n\n    def __init__(self, capacity: int):\n        if capacity <= 0:\n            raise ValueError(\"Capacity must be a positive integer\")\n        self.capacity = capacity\n        self.cache = {}  # key -> node\n        # Dummy head and tail to avoid empty checks\n        self.head = self._Node()\n        self.tail = self._Node()\n        self.head.next = self.tail\n        self.tail.prev = self.head\n        self.size = 0\n\n    def _add_node(self, node: _Node) -> None:\n        # Add node right after head (most-recent position)\n        node.prev = self.head\n        node.next = self.head.next\n        self.head.next.prev = node\n        self.head.next = node\n\n    def _remove_node(self, node: _Node) -> None:\n        # Remove an existing node from the linked list\n        prev = node.prev\n        nxt = node.next\n        prev.next = nxt\n        nxt.prev = prev\n        node.prev = node.next = None\n\n    def _move_to_head(self, node: _Node) -> None:\n        # Move given node to the most-recent position\n        self._remove_node(node)\n        self._add_node(node)\n\n    def _pop_tail(self) -> _Node:\n        # Remove and return the least-recently used node\n        node = self.tail.prev\n        self._remove_node(node)\n        return node\n\n    def get(self, key: int) -> int:\n        node = self.cache.get(key)\n        if not node:\n            return -1\n        # Move accessed node to head (most recent)\n        self._move_to_head(node)\n        return node.val\n\n    def put(self, key: int, value: int) -> None:\n        node = self.cache.get(key)\n        if node:\n            # Update value and move to head\n            node.val = value\n            self._move_to_head(node)\n        else:\n            # Insert new node\n            new_node = self._Node(key, value)\n            self.cache[key] = new_node\n            self._add_node(new_node)\n            self.size += 1\n            if self.size > self.capacity:\n                # Evict least recently used\n                tail = self._pop_tail()\n                del self.cache[tail.key]\n                self.size -= 1\n\n    def __repr__(self) -> str:\n        # Show pairs in access order: most-recent first\n        pairs = []\n        cur = self.head.next\n        while cur is not self.tail:\n            pairs.append((cur.key, cur.val))\n            cur = cur.next\n        return f\"LRUCache({pairs})\"",
    "reasoning-math": "P = 1 - \u220f_{k=0}^{22} (365 - k)/365\n  = 1 - (365/365)*(364/365)*...*(343/365)\n  = 1 - 0.4927027656760144\n  = 0.5072972343239856 \u2248 0.5073",
    "comprehension-read": "1. It returns a dict that maps each category to the average (mean) of the 'value' fields for items in that category. Missing 'category' is treated as 'unknown' and missing 'value' is treated as 0.\n\n2. An empty dict in data is treated as an item with no category and no value: it contributes to category 'unknown' and the value 0 is appended (affecting the 'unknown' average).\n\n3. If a category has zero items it simply does not appear in the returned dict (no average is computed for it).\n\n4. A ZeroDivisionError would occur if any category in result has an empty list (len(v) == 0), because sum(v) / len(v) would divide by zero. With normal inputs to this function (a fresh local result built from the provided items) that should not happen, since a key is only created when an item for that category is found. However, if result were prepopulated or otherwise left with an empty list for a key (e.g. result = {'a': []}) the comprehension would raise ZeroDivisionError.",
    "planning-task": "1) Database schema change (SQL)\n- Preferred: create a dedicated preferences table (safe, minimal disruption):\n  CREATE TABLE IF NOT EXISTS user_preferences (\n    user_id INTEGER PRIMARY KEY REFERENCES users(id),\n    theme TEXT NOT NULL DEFAULT 'system' CHECK(theme IN ('light','dark','system')),\n    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP\n  );\n- Alternatively (if you prefer a column on users and ALTER TABLE is acceptable):\n  ALTER TABLE users ADD COLUMN theme TEXT DEFAULT 'system';\n  -- Ensure application enforces allowed values ('light','dark','system') at app-level in this case.\n\n2) API endpoints needed (URL + method)\n- GET /api/user/theme  (GET)\n  - Returns: JSON { \"theme\": \"light\" | \"dark\" | \"system\" } for the authenticated user.\n  - Use: client can query current saved preference after load if needed.\n  - Auth: requires authenticated session/JWT.\n- PATCH /api/user/theme  (PATCH)\n  - Request body: JSON { \"theme\": \"light\" | \"dark\" | \"system\" }\n  - Action: upserts user_preferences.theme for current user (or updates users.theme column).\n  - Response: 200 with updated preference or 400 on invalid value.\n  - Auth: requires authenticated session/JWT; include CSRF protection if using cookie sessions.\n- (Optional) GET /api/theme/default  (GET)\n  - Returns server-side default theme (e.g., { \"default\": \"light\" }) if you want clients to respect a serverwide default; not strictly necessary.\n\n3) Frontend components (HTML / CSS / JS) \u2014 what each does\n- HTML (Jinja2 template changes)\n  - Add a theme toggle control somewhere (header/footer): a button with data attributes, e.g. <button id=\"theme-toggle\" aria-pressed=\"false\" data-theme=\"system\">Theme</button>.\n  - Add a short inline script at the very top of <head> (executed before CSS) that:\n    - reads preference in order: server-injected user preference (if available), else localStorage/cookie, else falls back to \"system\",\n    - applies the corresponding class or data-attribute to document.documentElement (e.g., document.documentElement.dataset.theme = 'dark').\n    - This prevents flash-of-incorrect-theme (FOUC).\n  - Optionally: when rendering for authenticated users, inject the saved preference into the page context (e.g., <script>window.__USER_THEME = \"{{ user_theme or '' }}\";</script>) so the inline script can use it without a fetch.\n- CSS (variables + theme classes + prefers-color-scheme)\n  - Define color variables under :root for defaults:\n    - :root { --bg: #fff; --text: #000; ... }\n  - Define .theme-dark (or [data-theme=\"dark\"]) overrides:\n    - .theme-dark { --bg: #111; --text: #eee; ... }\n  - For system (when no explicit theme), use prefers-color-scheme:\n    - @media (prefers-color-scheme: dark) { :root:not([data-theme]) { --bg: #111; --text: #eee; } }\n  - Use variables in your main styles: body { background: var(--bg); color: var(--text); }\n- JS (vanilla) \u2014 client behavior\n  - Initialization (run as early as possible):\n    - Determine effectiveTheme in order: server-injected theme -> localStorage / cookie -> 'system'.\n    - Apply document.documentElement.dataset.theme = effectiveTheme (use data-theme or class).\n  - Toggle handler:\n    - On theme-toggle click, cycle or switch between light/dark/system.\n    - Immediately update dataset/class to change UI.\n    - Persist change:\n      - If authenticated: send PATCH /api/user/theme with JSON body to save in SQLite; handle errors (retry or fallback to local persistence).\n      - If unauthenticated: save to localStorage (and optionally a non-sensitive cookie) so preference persists on that device.\n    - Update button aria attributes and visuals.\n  - Optional progressive enhancement:\n    - If user is authenticated but inline injection not available, fetch GET /api/user/theme once and apply.\n  - Edge cases:\n    - If user selects 'system', remove explicit theme class so CSS prefers-color-scheme applies.\n\n4) Default/inheritance behavior (priority order)\n- 1) Explicit user account preference (database) \u2014 highest priority for authenticated users.\n- 2) Page-level override (a template or page can set data-theme on <html> to force a particular theme for that page).\n- 3) Local client preference for anonymous users (localStorage or cookie).\n- 4) 'system' preference: if effective preference is 'system' or none of the above, respect the OS/browser prefers-color-scheme media query.\n- 5) Server-wide default (fallback): if no other information, use a defined server default (commonly 'light').\n\n5) Cross-device persistence (one sentence)\n- For authenticated users the theme choice is stored in SQLite tied to the user account and served via API, so once saved it follows the user across devices after they sign in; for anonymous users the choice is stored locally (localStorage/cookie) and remains device-specific."
  }
}