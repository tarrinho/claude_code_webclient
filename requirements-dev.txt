# Development, CI, and security tooling — not required at runtime.
httpx>=0.28,<1
bandit>=1.7,<2
safety>=3.0,<4
pip-audit>=2.7,<3
ruff>=0.9,<1
# Coverage: the suite had no line-coverage tool, so gaps were only findable by
# reading code. pytest-cov wires it into the existing pytest invocation.
coverage>=7.4,<8
pytest-cov>=5.0,<7
# Pure-Python JS parser. There is no node on the dev box, so app.js was only
# ever checked by substring assertions -- a syntax error would ship green.
esprima>=4.0,<5
