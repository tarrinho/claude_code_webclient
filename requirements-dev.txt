# Development, CI, and security tooling — not required at runtime.
httpx>=0.28,<1
bandit>=1.9.4,<2
safety>=3.0,<4
pip-audit>=2.7,<3
ruff>=0.9,<1
# Coverage: the suite had no line-coverage tool, so gaps were only findable by
# reading code. pytest-cov wires it into the existing pytest invocation.
coverage>=7.4,<8
pytest-cov>=5.0,<7
# JS parser for the frontend syntax gate. There is no node on the dev box, so
# app.js was only ever checked by substring assertions and a syntax error would
# ship green. QuickJS rather than esprima: esprima's grammar predates optional
# catch binding (ES2019), which this codebase uses, so it rejects valid source.
quickjs>=1.19,<2
